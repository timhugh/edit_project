name: Publish Release

on:
  push:
    tags:
      - 'v*'

jobs:
  test:
    name: Test on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-go@v4
        with:
          go-version: '1.25.5'
      - run: go test ./...

  build:
    name: Build and package ${{ matrix.os }}-${{ matrix.arch }}
    needs: test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        os: [linux, windows, darwin]
        arch: [amd64, arm64]
        package_format: [tar.gz, zip]
        exclude:
          - os: windows
            package_format: tar.gz
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-go@v4
        with:
          go-version: '1.25.5'
      - name: read version file
        id: read_version
        run: echo "VERSION=$(cat VERSION)" >> $GITHUB_ENV
      - name: build binary for ${{ matrix.os }}-${{ matrix.arch }}
        env:
          VERSION: ${{ env.VERSION }}
          GOOS: ${{ matrix.os }}
          GOARCH: ${{ matrix.arch }}
        run: |
          mkdir -p dist
          output="edit-cli-${{ matrix.os }}-${{ matrix.arch }}"
          if [ "${{ matrix.os }}" = "windows" ]; then
            output="${output}.exe"
          fi
          go build -o dist/"${output}" ./cmd/edit-cli
      - name: package binary
        run: |
          cd dist
          case "${{ matrix.package_format }}" in
            tar.gz)
              tar -czf edit-cli-${{ matrix.os }}-${{ matrix.arch }}.tar.gz edit-cli-${{ matrix.os }}-${{ matrix.arch }}
              ;;
            zip)
              zip edit-cli-${{ matrix.os }}-${{ matrix.arch }}.zip edit-cli-${{ matrix.os }}-${{ matrix.arch }}
              ;;
          esac
      - name: upload artifact
        uses: actions/upload-artifact@v3
        with:
          name: edit-cli-${{ matrix.os }}-${{ matrix.arch }}.${{ matrix.package_format }}
          path: dist/edit-cli-${{ matrix.os }}-${{ matrix.arch }}.${{ matrix.package_format }}

  release:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/download-artifact@v3
        with:
          path: dist
      - uses: actions/create-release@v1
        with:
          tag_name: ${{ github.ref_name }}
          release_name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false
      - run: gh release upload ${{ github.ref_name }} dist/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

